# Frontend (React + TS + Vite) — scaffold next after backend demo.
# Planned screens: Overview, Code Review (finding/severity/confidence/file/line/evidence/rule),
# Pentest Reliability (expected/completed/failed/unverified/evidence/consistency/recovery), Finding Details.
# Keep read-only; backend remains source of truth. Run `npm create vite@latest frontend -- --template react-ts` here.

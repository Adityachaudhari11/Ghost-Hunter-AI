# Demo target repo — owned, intentionally small.

All database operations must go through the Repository layer.
Authentication must go through AuthService.
Logging must use the project's structured logger.
Reuse existing utilities where applicable — do not reimplement date helpers.
